<div class="container">
  <article class="container-inner">
    <section>
      <h1 class="title">denene</h1>
      <p class="description">
        「控えめな Web」を探究し、標準と新しいさを見つける
      </p>
      <a
        href="https://jamband.github.io/denene/"
        class="link"
        target="_blank"
        rel="noreferrer">go to denene</a
      >
    </section>
    <div class="screenshots">
      <picture>
        <source
          srcset={`${
            import.meta.env.BASE_URL
          }denene/first-view-screenshot-sm.webp`}
          type="image/webp"
        />
        <img
          src={`${import.meta.env.BASE_URL}denene/first-view-screenshot-sm.jpg`}
          width="379"
          height="674"
          class="screenshot-sm"
          alt="screenshot for mobile phone"
        />
      </picture>
      <picture>
        <source
          srcset={`${
            import.meta.env.BASE_URL
          }denene/first-view-screenshot.webp`}
          type="image/webp"
        />
        <img
          src={`${import.meta.env.BASE_URL}denene/first-view-screenshot.jpg`}
          width="1373"
          height="1069"
          class="screenshot"
          alt="screenshot for pc"
        />
      </picture>
    </div>
  </article>
</div>

<style>
  .container {
    background: linear-gradient(
      to bottom,
      rgb(var(--gray-800)),
      rgb(var(--gray-900))
    );
  }

  .container-inner {
    display: grid;
    height: 95dvh;
    margin: 0 auto;
    max-width: 1536px;
    padding: 0 0.5rem;
    place-content: center;
    place-items: center;
    text-align: center;
  }

  @media (min-width: 768px) {
    .container-inner {
      padding: 0 1rem;
      row-gap: 1rem;
    }
  }

  @media (min-width: 1024px) {
    .container-inner {
      column-gap: 4rem;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      padding: 0 6rem;
    }
  }

  .title {
    color: rgb(var(--gray-100));
    font-size: 2.75rem;
    font-weight: bold;
    line-height: 1.2;
    margin-bottom: 1rem;
  }

  .description {
    margin-bottom: 2rem;
    padding: 0 0.5rem;
  }

  @media (min-width: 768px) {
    .description {
      padding: 0 6rem;
    }
  }

  @media (min-width: 1024px) {
    .description {
      padding: 0;
    }
  }

  .link {
    background: rgb(var(--gray-400) / 10%);
    border: 2px solid rgb(var(--gray-400) / 80%);
    border-radius: 0.125rem;
    box-shadow: 0 14px 32px -12px rgb(var(--gray-400) / 20%);
    color: rgb(var(--gray-100));
    display: inline-block;
    font-weight: bold;
    margin-bottom: 3rem;
    padding: 0.5rem 1rem;
  }

  .link:active {
    background: rgb(var(--gray-400) / 15%);
  }

  @media (hover: hover) and (pointer: fine) {
    .link:hover {
      background: rgb(var(--gray-400) / 15%);
    }
  }

  .screenshots {
    display: flex;
    justify-content: center;
    position: relative;
  }

  .screenshot,
  .screenshot-sm {
    background: rgb(var(--gray-800));
    border: 1px solid rgb(var(--gray-400) / 20%);
    border-radius: 0.25rem;
    height: auto;
    max-width: 64%;
  }

  @media (min-width: 768px) {
    .screenshot {
      max-width: 50%;
    }
  }

  @media (min-width: 1024px) {
    .screenshot {
      max-width: 100%;
    }
  }

  .screenshot-sm {
    bottom: -4%;
    left: 15%;
    max-width: 22%;
    position: absolute;
  }

  @media (min-width: 768px) {
    .screenshot-sm {
      bottom: -3%;
      left: 23%;
      max-width: 15%;
    }
  }

  @media (min-width: 1024px) {
    .screenshot-sm {
      bottom: -3%;
      left: -3%;
      max-width: 28%;
    }
  }
</style>
