---
import IconCode from "@/icons/icon-code.astro";
import IconGithub from "@/icons/icon-github.astro";
---

<div class="container">
  <article class="container-inner">
    <section class="header">
      <h2 class="header-title">
        Technology<br class="header-title-new-line" aria-hidden="true" />
        Stack
      </h2>
      <p class="header-description">
        tapes
        の技術スタックを紹介します。以下のリストは現在のものであって、時間とともに変化していく可能性があります
      </p>
    </section>
    <div class="separator" role="separator">
      <IconCode class="separator-icon" />
    </div>
    <article class="main">
      <section>
        <h3 class="main-title">Frontend</h3>
        <ul class="main-tag-list">
          <li class="main-tag-list-item">TypeScript</li>
          <li class="main-tag-list-item">SvelteKit</li>
          <li class="main-tag-list-item">Tailwind CSS</li>
          <li class="main-tag-list-item">ESLint</li>
          <li class="main-tag-list-item">Prettier</li>
        </ul>
      </section>
      <section>
        <h3 class="main-title">Others</h3>
        <ul class="main-tag-list">
          <li class="main-tag-list-item">GitHub Actions</li>
          <li class="main-tag-list-item">GitHub Pages</li>
        </ul>
      </section>
    </article>
    <section class="footer">
      <p class="footer-description">
        tapes のコードはオープンソースとして GitHub
        で管理しています。より詳しい内容については以下のリンクから見ることができます
      </p>
      <a
        href="https://github.com/jamband/tapes"
        class="footer-link"
        target="_blank"
        rel="noreferrer"><IconGithub class="footer-link-icon" />Frontend</a
      >
    </section>
  </article>
</div>

<style>
  .container {
    background: linear-gradient(
      to bottom,
      rgb(var(--gray-800)),
      rgb(var(--gray-900))
    );
  }

  .container-inner {
    padding: 6rem 1rem;
  }

  @media (width >= 768px) {
    .container-inner {
      margin: 0 auto;
      max-width: 768px;
    }
  }

  @media (width >= 1024px) {
    .container-inner {
      max-width: 1024px;
    }
  }

  .header {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    text-align: center;
  }

  .header-title {
    color: rgb(var(--gray-100));
    font-size: 2.5rem;
    font-weight: bold;
    line-height: 1.2;
  }

  @media (width >= 768px) {
    .header-title-new-line {
      display: none;
    }
  }

  .header-description {
    line-height: 1.85;
  }

  @media (width >= 768px) {
    .header-description {
      padding: 0 2rem;
    }
  }

  @media (width >= 1024px) {
    .header-description {
      padding: 0 8rem;
    }
  }

  .separator {
    display: flex;
    justify-content: center;
    margin-top: 3rem;
  }

  .separator-icon {
    height: 1.75rem;
    width: 1.75rem;
  }

  .main {
    display: grid;
    gap: 4rem;
    margin-top: 3rem;
  }

  @media (width >= 768px) {
    .main {
      gap: 4rem 3rem;
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  .main-title {
    color: rgb(var(--gray-100));
    font-size: 2rem;
    font-weight: bold;
    line-height: 1.2;
  }

  .main-tag-list {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem 0.75rem;
    margin-top: 1rem;
  }

  .main-tag-list-item {
    background: rgb(var(--purple-400) / 10%);
    border: 1px solid rgb(var(--purple-400) / 20%);
    border-radius: 9999px;
    color: rgb(var(--purple-400));
    font-size: 1rem;
    line-height: 1.25;
    padding: 0.4rem 1rem;
  }

  .footer {
    align-items: center;
    display: flex;
    flex-direction: column;
    gap: 2rem;
    justify-content: center;
    margin: 5rem auto 0;
    max-width: 768px;
    text-align: center;
  }

  .footer-description {
    line-height: 1.85;
  }

  @media (width >= 768px) {
    .footer-description {
      padding: 0 4rem;
    }
  }

  .footer-link {
    align-items: center;
    background: rgb(var(--gray-400) / 10%);
    border: 2px solid rgb(var(--gray-400) / 80%);
    border-radius: 0.5rem;
    box-shadow: 0 14px 32px -12px rgb(var(--gray-400) / 20%);
    color: rgb(var(--gray-100));
    display: inline-flex;
    font-weight: bold;
    gap: 0.3rem;
    padding: 0.5rem 1rem;
  }

  .footer-link:active {
    background: rgb(var(--gray-400) / 15%);
  }

  @media (hover: hover) and (pointer: fine) {
    .footer-link:hover {
      background: rgb(var(--gray-400) / 15%);
    }
  }

  .footer-link-icon {
    height: 1.25rem;
    width: 1.25rem;
  }
</style>
