---
import IconArrowLeft from "@/icons/icon-arrow-left.astro";
import IconCode from "@/icons/icon-code.astro";
---

<div class="container">
  <h1 class="title">Not Found</h1>
  <p class="description">
    このページはすでに削除されているか、もしくはページが存在しません
  </p>
  <IconCode class="separator" />
  <a class="link" href="/about/"
    ><IconArrowLeft class="link-icon" />トップページに戻る</a
  >
</div>

<style>
  .container {
    align-items: center;
    display: flex;
    flex-direction: column;
    height: 100dvh;
    justify-content: center;
    padding: 0 0.25rem;
    text-align: center;
  }

  .title {
    color: rgb(var(--gray-100));
    font-size: 3.5rem;
    font-weight: bold;
    line-height: 1.2;
    margin-bottom: 0.5rem;
  }

  .description {
    line-height: 1.85;
    margin-bottom: 2rem;
  }

  .separator {
    height: 1.75rem;
    margin-bottom: 2.5rem;
    width: 1.75rem;
  }

  .link {
    align-items: center;
    background: rgb(var(--sky-500) / 10%);
    border: 1px solid rgb(var(--sky-500) / 20%);
    border-radius: 0.5rem;
    box-shadow: 0 14px 32px -12px rgb(0 153 255 / 20%);
    color: rgb(var(--sky-500));
    display: inline-flex;
    font-weight: bold;
    gap: 0.5rem;
    padding: 0.5rem 1.5rem;
  }

  .link:active {
    background: rgb(var(--sky-500) / 20%);
    border: 1px solid rgb(var(--sky-500) / 30%);
  }

  @media (hover: hover) and (pointer: fine) {
    .link:hover {
      background: rgb(var(--sky-500) / 20%);
      border: 1px solid rgb(var(--sky-500) / 30%);
    }
  }

  .link-icon {
    height: 1rem;
    width: 1rem;
  }
</style>
